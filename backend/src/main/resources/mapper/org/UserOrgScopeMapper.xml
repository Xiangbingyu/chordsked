<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chordsked.backend.dao.mapper.UserOrgScopeMapper">

    <select id="listByUserId" resultType="com.chordsked.backend.model.entity.UserOrgScopeEntity">
        SELECT id,
               user_id,
               org_node_id,
               is_primary,
               created_at,
               updated_at
        FROM sys_user_org_scope
        WHERE user_id = #{userId}
        ORDER BY is_primary DESC, org_node_id ASC
    </select>

    <select id="listByOrgNodeId" resultType="com.chordsked.backend.model.entity.UserOrgScopeEntity">
        SELECT id,
               user_id,
               org_node_id,
               is_primary,
               created_at,
               updated_at
        FROM sys_user_org_scope
        WHERE org_node_id = #{orgNodeId}
        ORDER BY is_primary DESC, user_id ASC
    </select>

    <select id="listUserIdsByOrgNodeIds" resultType="long">
        SELECT DISTINCT user_id
        FROM sys_user_org_scope
        WHERE org_node_id IN
        <foreach collection="orgNodeIds" item="orgNodeId" open="(" separator="," close=")">
            #{orgNodeId}
        </foreach>
        ORDER BY user_id ASC
    </select>

    <delete id="deleteByUserId">
        DELETE FROM sys_user_org_scope
        WHERE user_id = #{userId}
    </delete>

    <delete id="deleteByUserIdAndOrgNodeId">
        DELETE FROM sys_user_org_scope
        WHERE user_id = #{userId}
          AND org_node_id = #{orgNodeId}
    </delete>

    <insert id="saveBatch">
        INSERT INTO sys_user_org_scope (user_id, org_node_id, is_primary, created_at, updated_at)
        VALUES
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.orgNodeId}, #{entity.isPrimary}, #{entity.createdAt}, #{entity.updatedAt})
        </foreach>
    </insert>

    <select id="countPrimaryByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM sys_user_org_scope
        WHERE user_id = #{userId}
          AND is_primary = 1
    </select>

    <select id="getPrimaryOrgNodeIdByUserId" resultType="long">
        SELECT org_node_id
        FROM sys_user_org_scope
        WHERE user_id = #{userId}
          AND is_primary = 1
        LIMIT 1
    </select>

</mapper>
