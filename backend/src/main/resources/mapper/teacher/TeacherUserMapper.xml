<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chordsked.backend.dao.mapper.TeacherUserMapper">

    <sql id="TeacherUserColumns">
        id, teacher_no, name, phone, password, avatar, teacher_level, campus_id, status,
        must_change_password, last_login_at, experience, good_at,
        created_at, updated_at
    </sql>

    <select id="getById" resultType="com.chordsked.backend.model.entity.TeacherUserEntity">
        SELECT
        <include refid="TeacherUserColumns"/>
        FROM sys_teacher_user
        WHERE id = #{userId}
          AND status != 4
    </select>

</mapper>
