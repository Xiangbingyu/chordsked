<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chordsked.backend.dao.mapper.UserCampusMapper">

    <select id="selectPrimaryCampusIdByUserId" resultType="long">
        SELECT campus_id
        FROM sys_user_campus
        WHERE user_id = #{userId}
          AND is_primary = 1
        LIMIT 1
    </select>

    <select id="listCampusIdsByUserId" resultType="long">
        SELECT campus_id
        FROM sys_user_campus
        WHERE user_id = #{userId}
        ORDER BY is_primary DESC, campus_id ASC
    </select>

    <delete id="deleteByUserId">
        DELETE FROM sys_user_campus
        WHERE user_id = #{userId}
    </delete>

    <insert id="saveBatch">
        INSERT INTO sys_user_campus (user_id, campus_id, is_primary, created_at, updated_at)
        VALUES
        <foreach collection="campusIds" item="campusId" separator=",">
            (#{userId}, #{campusId}, CASE WHEN #{campusId} = #{primaryCampusId} THEN 1 ELSE 0 END, #{now}, #{now})
        </foreach>
    </insert>

    <select id="countByCampusId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM sys_user_campus
        WHERE campus_id = #{campusId}
    </select>

</mapper>
