
backend/src/main/java/com/chordsked/backend/exception/ErrorCode.java (Diff revision 1)
    UNAUTHORIZED(40100, "未认证或令牌无效"),
 An issue was opened.Show all issues
这两个是通过http的请求状态码返回的

 Reply
backend/src/main/java/com/chordsked/backend/security/account/model/ChordSkedUserDetails.java (Diff revision 1)
public class ChordSkedUserDetails implements UserDetails {
 An issue was opened.Show all issues
缺少以下接口实现
@Override
public boolean isAccountNonExpired() {
    // 当前为静态权限占位，后续接入数据库时需检查账号过期时间
    return true;
}


@Override
public boolean isAccountNonLocked() {
    // 当前为静态权限占位，后续需实现登录失败锁定逻辑
    return true;
}


@Override
public boolean isCredentialsNonExpired() {
    // 当前为静态权限占位，后续需实现密码过期策略
    return true;
}


@Override
public boolean isEnabled() {
    // 当前为静态权限占位，后续需检查账号 ACTIVE/INACTIVE 状态
    return true;
}

 Reply
backend/src/main/java/com/chordsked/backend/security/account/provider/InternalAccountProvider.java (Diff revision 1)
            new SimpleGrantedAuthority("user:view"),
 An issue was opened.Show all issues
1.在api路径前缀中需要需要区分3端的api
然后在 SecurityConfig 中使用：
 .requestMatchers("/api/v1/admin/*").hasAnyRole("ADMIN")   角色检查，让不同端账号只能访问对应端的api


2.前面再加个前缀。类似这种admin:user:view，
用来区分3端账号，后续用户端为stundent::
教务端账号的god账号为admin::，教务端普通账号按照具体的权限树结构去生成
教师端为teacher::
兼容后续教师端可能要求有角色权限管理
有一些共用的api可以不加api权限校验，只要通过角色权限校验即可



3.不要出现ROLE_ADMIN、ADMIN这样2种角色权限写法

 Reply
backend/src/main/java/com/chordsked/backend/security/jwt/JwtAuthenticationFilter.java (Diff revision 1)
                                String.valueOf(userId)
 An issue was opened.Show all issues
统一下数据类型，不要进行这种转化，如果细节实现上觉得那种类型更好就用哪种

 Reply
backend/src/main/java/com/chordsked/backend/security/jwt/JwtAuthenticationFilter.java (Diff revision 1)
            } catch (RuntimeException ignored) {
 An issue was opened.Show all issues
捕获所有异常但不记录日志，调试困难
无法区分是 Token 过期、签名错误还是其他问题
生产环境出现问题无法排查
} catch (ExpiredJwtException e) {
    // 记录 Token 过期（调试级别）
    logger.debug("JWT token expired for request: {}", request.getRequestURI());
} catch (MalformedJwtException e) {
    // 记录无效 Token（警告级别）
    logger.warn("Malformed JWT token in request: {}", request.getRequestURI());
} catch (RuntimeException e) {
    //  记录其他异常（错误级别）
    logger.error("JWT authentication failed for request: {}", request.getRequestURI(), e);
}

 Reply
backend/src/main/java/com/chordsked/backend/utils/jwt/JwtTokenUtils.java (Diff revision 1)
            Claims claims = parseClaims(token);
 An issue was opened.Show all issues
缺少过期时间校验，

 Reply
backend/src/main/java/com/chordsked/backend/utils/jwt/JwtTokenUtils.java (Diff revision 1)
            return false;
 An issue was opened.Show all issues
缺少日志，所有异常捕获的地方都要打印出捕获到的异常

 Reply

 backend/src/main/java/com/chordsked/backend/controller/StudentController.java (Diff revision 1)
@RequestMapping("/api/v1/students")
@RequestMapping("/api/v1/admin/students")
 An issue was opened.Show all issues
使用  students/api/v1

 Reply
backend/src/main/java/com/chordsked/backend/security/account/model/ChordSkedUserDetails.java (Diff revision 1)
    private final String userType;
 An issue was opened.Show all issues
这个使用enum

 Reply

 backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    updated_at BIGINT NOT NULL,
    avatar VARCHAR(200) NULL COMMENT '头像URL',
 An issue was opened.Show all issues
avatar这类图片，未来统一放在阿里云oss

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    PRIMARY KEY (id)
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态(0:禁用 1:启用)',
 An issue was opened.Show all issues
is_deleted可以删除，作为status的一个状态

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    login_fail_count INT NOT NULL DEFAULT 0 COMMENT '连续登录失败次数',
 An issue was opened.Show all issues
login_fail_count和locked_until 的功能可以做到redis ，不需要做到mysql中

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    locked_until DATETIME NULL COMMENT '账号锁定截止时间',
 An issue was opened.Show all issues
数据库中的时间戳都统一改成  毫秒级时间戳BIGINT，譬如created_at ， updated_at

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    is_deleted TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除(1:是 0:否)',
 An issue was opened.Show all issues
同上

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    teaching_type VARCHAR(20) NOT NULL COMMENT '授课类型(ONE_ON_ONE/SMALL/LARGE/ALL)',
 An issue was opened.Show all issues
这个字段不需要，默认所有老师可以授各类课型

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    teacher_level VARCHAR(20) NOT NULL COMMENT '教师等级(JUNIOR/INTERMEDIATE/SENIOR/EXPERT)',
 An issue was opened.Show all issues
修改为TINYINT类型的枚举

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    login_fail_count INT NOT NULL DEFAULT 0 COMMENT '连续登录失败次数',
 An issue was opened.Show all issues
同上

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    created_at DATETIME NOT NULL COMMENT '创建时间',
 An issue was opened.Show all issues
同上

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    is_deleted TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除(1:是 0:否)',
 An issue was opened.Show all issues
同上

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '权限ID',
 An issue was opened.Show all issues
权限树还是维护在代码里面，可以通过枚举树来表示
这块未来变动会比较大，如果放在数据库会导致后期扫档频繁

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    user_type VARCHAR(20) NOT NULL COMMENT '用户类型(INTERNAL/TEACHER/STUDENT)',
 An issue was opened.Show all issues
TINYINT

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
CREATE TABLE IF NOT EXISTS sys_sms_code (
 An issue was opened.Show all issues
这个功能可以放在redis， 不需要放在mysql

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    target_type VARCHAR(20) NOT NULL COMMENT '目标类型(ROLE/USER)',
 An issue was opened.Show all issues
TINYINT

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    scope_type VARCHAR(20) NOT NULL COMMENT '范围类型(ALL/SPECIFIED/CURRENT/SELF)',
 An issue was opened.Show all issues
TINYINT

 Reply
Fix it!
admin
8 hours, 43 minutes ago
 Comment
backend/src/main/java/com/chordsked/backend/controller/StudentController.java (Diff revision 1)
        return ApiResponse.success(studentService.listStudents(keyword, level, page, pageSize));
        StudentListRequest request = new StudentListRequest();
 An issue was opened.Show all issues
controller层先做参数校验

 Reply
backend/src/main/java/com/chordsked/backend/dao/impl/StudentUserDaoMBImpl.java (Diff revision 1)
    public List<StudentUserEntity> listStudents(String keyword, Integer status, int offset, int limit) {
 An issue was opened.Show all issues
dao层不要忘记参数校验

 Reply
backend/src/main/java/com/chordsked/backend/model/entity/DataScopeEntity.java (Diff revision 1)
    public Long getId() { return id; }
 An issue was opened.Show all issues
代码格式

 Reply
backend/src/main/java/com/chordsked/backend/model/entity/InternalUserEntity.java (Diff revision 1)
    private Integer loginFailCount;
 An issue was opened.Show all issues
使用enum

 Reply
backend/src/main/java/com/chordsked/backend/model/entity/InternalUserEntity.java (Diff revision 1)
    private LocalDateTime lockedUntil;
 An issue was opened.Show all issues
这类需要改成毫秒时间戳，代码层使用Long

 Reply
backend/src/main/java/com/chordsked/backend/model/entity/LoginLogEntity.java (Diff revision 1)
    public Long getId() { return id; }
 An issue was opened.Show all issues
注意代码格式

 Reply
backend/src/main/java/com/chordsked/backend/model/entity/PermissionEntity.java (Diff revision 1)
    public Long getId() { return id; }
 An issue was opened.Show all issues
注意代码格式

 Reply
backend/src/main/java/com/chordsked/backend/model/enums/TeacherUserLevelType.java (Diff revision 1)
    JUNIOR,
 An issue was opened.Show all issues
枚举尽量统一格式，需要设置具体 code，不要用name比较

 Reply
backend/src/main/java/com/chordsked/backend/model/enums/TeacherUserTeachingType.java (Diff revision 1)
    LARGE,
 An issue was opened.Show all issues
同上

 Reply
backend/src/main/java/com/chordsked/backend/model/vo/StudentListResultVO.java (Diff revision 1)
    private Integer status;
 An issue was opened.Show all issues
vo定义中，枚举尽量用enum类型

 Reply
backend/src/main/java/com/chordsked/backend/security/account/provider/InternalAccountProvider.java (Diff revision 1)
    private SecurityAccountMapper securityAccountMapper;
 An issue was opened.Show all issues
service层不要直接调用mapper，需要调用dao interface

 Reply
backend/src/main/java/com/chordsked/backend/security/account/provider/StudentAccountProvider.java (Diff revision 1)
    );
    private SecurityAccountMapper securityAccountMapper;
 An issue was opened.Show all issues
同上

 Reply
backend/src/main/java/com/chordsked/backend/security/account/provider/TeacherAccountProvider.java (Diff revision 1)
    );
    private SecurityAccountMapper securityAccountMapper;
 An issue was opened.Show all issues
同上

 Reply
backend/src/main/java/com/chordsked/backend/security/jwt/JwtAuthenticationFilter.java (Diff revision 1)
    @Autowired(required = false)
    @Autowired(required = false)
 An issue was opened.Show all issues
指定bean

 Reply
backend/src/main/java/com/chordsked/backend/security/jwt/JwtAuthenticationFilter.java (Diff revision 1)
    @Autowired(required = false)
    @Autowired(required = false)
 An issue was opened.Show all issues
使用resource，制定具体的bean

 Reply
backend/src/main/java/com/chordsked/backend/security/jwt/JwtAuthenticationFilter.java (Diff revision 1)
    @Autowired(required = false)
 An issue was opened.Show all issues
同上

 Reply
backend/src/main/java/com/chordsked/backend/security/jwt/JwtAuthenticationFilter.java (Diff revision 1)
                && multiAccountUserDetailsService != null
                && multiAccountUserDetailsService != null
 An issue was opened.Show all issues
确定注入顺序，保证各个bean已经配置成功

 Reply
backend/src/main/java/com/chordsked/backend/service/impl/AuthLoginServiceImpl.java (Diff revision 1)
public class AuthLoginServiceImpl implements AuthLoginService {
 An issue was opened.Show all issues
service需要加事务

 Reply
backend/src/main/java/com/chordsked/backend/service/impl/AuthLoginServiceImpl.java (Diff revision 1)
    private InternalLoginMapper internalLoginMapper;
 An issue was opened.Show all issues
不要直接使用mapper

 Reply
backend/src/main/java/com/chordsked/backend/service/impl/AuthLoginServiceImpl.java (Diff revision 1)
            HttpServletRequest httpServletRequest,
 An issue was opened.Show all issues
service层尽量不要引入controller层特有的类似servlet包

 Reply
backend/src/main/java/com/chordsked/backend/service/impl/StudentListServiceImpl.java (Diff revision 1)
        int page = request.getPage() == null ? 1 : request.getPage();
 An issue was opened.Show all issues
service层和dao层类似，每个函数都需要做参数校验，譬如request是否为空

 Reply
backend/src/main/resources/mapper/security/SecurityAccountMapper.xml (Diff revision 1)
    <select id="selectInternalUserById" resultType="com.chordsked.backend.model.entity.InternalUserEntity">
 An issue was opened.Show all issues
每个数据库表，对应一个mapper，
不要让多个mapper对应一张表，未来表结构变动，会忘记改某些mapper

 Reply

 
backend/src/main/java/com/chordsked/backend/model/dto/LoginRequest.java (Diff revision 1)
public class LoginRequest {
 An issue was opened.Show all issues
添加长度校验

 Reply
backend/src/main/java/com/chordsked/backend/security/config/SecurityConfig.java (Diff revision 1)
                .csrf(AbstractHttpConfigurer::disable)
 An issue was opened.Show all issues
添加注释说明，列如为什么要禁用csrf，方便日后维护与排查问题

 Reply
backend/src/main/java/com/chordsked/backend/security/config/SecurityConfig.java (Diff revision 1)
                        .anyRequest().permitAll()
 An issue was opened.Show all issues
改为.anyRequest().denyAll()，哪些路径下可以访问需要明确指明

 Reply
backend/src/main/java/com/chordsked/backend/security/jwt/JwtTokenProvider.java (Diff revision 1)
public class JwtTokenProvider {
 An issue was opened.Show all issues
添加 refresh token 支持，实现 token 续期

 Reply
backend/src/main/java/com/chordsked/backend/service/impl/AuthServiceImpl.java (Diff revision 1)
            throw new BadCredentialsException("student_id or password not correct");
 An issue was opened.Show all issues
登录失败没有日志记录，添加审计日志（注意不要记录明文密码）

 Reply
backend/src/main/java/com/chordsked/backend/service/impl/AuthServiceImpl.java (Diff revision 1)
    private String hashPassword(String rawPassword, String salt) {
 An issue was opened.Show all issues
1.改用 BCrypt 或 Argon2算法加密，安全性更高
2.这个方式抽出来成为utils中的共用方法

 Reply
frontend/src/utils/auth.ts (Diff revision 1)
  return localStorage.getItem(AUTH_TOKEN_KEY)
 An issue was opened.Show all issues
推荐使用 httpOnly cookie

 Reply

 backend/src/main/java/com/chordsked/backend/config/SecurityConfig.java (Diff revision 1)
    @Bean
 An issue was opened.Show all issues
bean 确认具体name

 Reply
backend/src/main/java/com/chordsked/backend/config/SecurityConfig.java (Diff revision 1)
    @Bean
 An issue was opened.Show all issues
bean 确认具体name

 Reply
backend/src/main/java/com/chordsked/backend/controller/AuthController.java (Diff revision 1)
    @Operation(summary = "用户注册")
 An issue was opened.Show all issues
swagger doc加上description

 Reply
backend/src/main/java/com/chordsked/backend/security/JwtTokenProvider.java (Diff revision 1)
    @Resource
 An issue was opened.Show all issues
申明具体的name

 Reply
backend/src/main/resources/db/h2/schema.sql (Diff revision 1)
CREATE TABLE IF NOT EXISTS `user` (
 An issue was opened.Show all issues
直接用student表吧

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
CREATE TABLE IF NOT EXISTS `user` (
 An issue was opened.Show all issues
直接用student表吧

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    username VARCHAR(64) NOT NULL,
 An issue was opened.Show all issues
每个字段加一下 COMMENT

 Reply
backend/src/main/resources/db/mysql/schema.sql (Diff revision 1)
    status TINYINT NOT NULL DEFAULT 1,
 An issue was opened.Show all issues
不要加 default，默认设置尽量写在代码里面

 Reply
backend/src/main/resources/env/test/application-test.yml (Diff revision 1)
      encoding: ${CHORDSKED_TEST_SQL_ENCODING:UTF-8}
 An issue was opened.Show all issues
不要修改test的mysql配置，未来测试服会用到这个数据库

 Reply
Fix it!
admin
2 weeks ago
 Comment
backend/src/main/java/com/chordsked/backend/model/entity/UserEntity.java (Diff revision 1)
    private Integer status;
 An issue was opened.Show all issues
这个字段用枚举，确认具体的状态
让mybatis自己转化成tinyint

 Reply
backend/src/main/resources/db/h2/schema.sql (Diff revision 1)
    id BIGINT NOT NULL AUTO_INCREMENT,
    id BIGINT NOT NULL AUTO_INCREMENT,
 An issue was opened.Show all issues
如果开发服不用h2 ，那就把schema.sql合并成一份，方便以后维护

 Reply
frontend/src/stores/authStore.ts (Diff revision 1)
  login: () => set({ loggedIn: true }),
  login: () => set({ loggedIn: true }),
 An issue was opened.Show all issues
token可以放在这个store中，这样hasValidToken 可以全局管理，逻辑在一处

 Reply